telegram:
  token: "NEW_TOKEN_WILL_BE_IN_DOTENV"
  admin_ids: [5317660746]
  
  
  request_kwargs:
    read_timeout: 60    # Увеличиваем
    connect_timeout: 60 # Увеличиваем
    pool_timeout: 60    # Увеличиваем
  
  proxy_url: null

lmstudio:
  api_url: "http://127.0.0.1:1234"
  timeout: 30
  max_tokens: 200

persona:
  default: "mark"
  use_lmstudio: true
